{% extends '::base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>{{ student.name }}</h1>
    <p>{{ "now"|date('Y-m-d H:i:s') }}</p>
    <p>{{ student.description }}</p>
{% endblock %}